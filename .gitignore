node_modules/
.next/
.env
.env.local
.vercel/
*.log
.DS_Store

# NOTE: data/status.json, data/history.json, data/latest/, pipeline/golden/ are
# committed by CI (they are the dashboard's state + the deliverable). Locally,
# `npm run pipeline` writes them; don't hand-edit. The *.fixture.json files are
# the checked-in dev fallback used before the first real run.

# next.js generated
next-env.d.ts
/CLAUDE.md
/AGENTS.md
.vercel
.env*
